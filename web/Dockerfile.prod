FROM node:22-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY . .

ENV NODE_ENV=production
ENV PORT=3005

RUN npm run build

EXPOSE 3005

CMD ["npm", "run", "start", "--", "--hostname", "0.0.0.0", "--port", "3005"]
